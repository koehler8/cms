# Site token used by some extensions and the provisioning tooling (safe to
# leave as-is for a plain content site).
VITE_TOKEN=__SLUG__

# Optional: override the framework version string reported in the client
# bundle (defaults to the installed @koehler8/cms package version).
# VITE_APP_VERSION=
